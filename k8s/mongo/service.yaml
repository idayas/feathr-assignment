apiVersion: v1
kind: Service
metadata:
  name: mongo
  labels:
    app: mongo
  namespace: feathr
spec:
  selector:
    app: mongo
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
