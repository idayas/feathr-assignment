apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: api 
  namespace: feathr
spec:
  selector:
    app: api 
  type: ClusterIP
  ports:
    - port: 5070
      targetPort: 5070
      protocol: TCP
